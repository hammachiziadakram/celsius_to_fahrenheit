# temperature-lab

## Function
`celsius_to_fahrenheit(celsius)`
Converts Celsius to Fahrenheit with validation.

## Rules
- Must accept int and float
- Must reject non-numeric input
- Must reject values below absolute zero (−273.15°C)

## Run tests
python -m unittest test_converter.py -v
